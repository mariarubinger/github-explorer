import { render } from 'react-dom';
import { App } from './App';

render(<App />, document.getElementById('root')) // recebe dois parâmetros: o primeiro é o que eu quero rendereizar ou seja exibir em tela, e o segundo em qual elemento quero renderizar
